# PROGRESS.md — 제작 일지

레미콘 운전기사용 개인 운행일지 PWA를 만들어온 기록이다.
"언제 무엇을 왜 바꿨는지"를 남기는 문서이고, 현재 앱이 어떻게 동작하는지는 `README.md`,
디자인 토큰과 색 규칙은 `DESIGN.md`를 본다.

되돌린 작업이나 실패한 시도도 지운 자리 없이 남긴다. 왜 지금 이 구조가 됐는지는
성공한 커밋보다 그쪽에 더 많이 담겨 있다.

---

## 한눈에 보기

| 시기 | 내용 |
|---|---|
| 2026-05-21 ~ 05-31 | HTML 파일 하나로 시작 |
| 2026-06-03 ~ 06-04 | `index.html` / `app.js` / `style.css` 로 분리 |
| 2026-06-08 ~ 06-10 | SpaceX 다크 테마 개편, PWA 오프라인, 사진 첨부 시도와 철회 |
| 2026-06-18 | 테마를 두 가지로 단순화 (한 번 되돌렸다 재적용) |
| 2026-06-27 ~ 06-30 | 모바일 조작감 다듬기, 기록 탭 제거 |
| 2026-07-01 ~ 07-06 | 월별 단가, 종료 km 자동 입력, 오티·톨비 상세, Pages 자동 배포 |
| 2026-07-22 | 디자인 개편, 현금거래(잔량 판매) 기능 |
| 2026-07-27 | 텍스트 크기 1.25배 확대 |
| 2026-08-01 | 달력에 공휴일·음력·절기 표시, 음력 기념일 등록 |
| 2026-08-01 | 달력 스와이프 시 blob 오류 화면 수정 (백업을 공유 시트로 교체) |
| 2026-08-01 | 업데이트 알림 팝업 + 설정탭 업데이트 내역 |
| 2026-08-02 | 종료 km 미입력 시 운행거리·유류량이 마이너스로 잡히던 버그 수정 |

---

## 2026-08-02 — 운행거리가 −2,446km로 나오던 버그

8월 통계 탭에서 아직 운행 기록이 없는데 **운행거리 −2,446km**, **이달 소비량 −1,345.3L**
로 표시됐다. 시작 km만 입력하고 종료 km는 아직 안 넣은 날이 있었던 것.

원인은 하루 운행거리를 여섯 군데에서 각자 `parseInt(ekm||0) - parseInt(skm||0)`로 계산하고
있었던 것이다. 종료 km가 비어 있으면 `0 - 2446 = -2446`이 되고, 유류 소비량은
`운행거리 × 연비계수`라서 같이 음수로 끌려갔다. 그래서 유류 정산 방향까지 뒤집혀
"도급비 차감"으로 표시됐다.

- `dayKm(v)` 헬퍼를 하나 만들어 **시작·종료 km이 둘 다 있고 종료 > 시작일 때만** 거리를 낸다
- 그 외(종료 미입력, 종료 < 시작)는 0
- 흩어져 있던 여섯 곳을 전부 `dayKm()`으로 교체:
  `fuelMonth` / `fuelMonthExclude` / `calcPrevKm` / 기록탭 `kmD` / 통계탭 `tkm`·`allKm` /
  연말결산 `km2`
- 입력 화면의 실시간 카드(`uFuelCard`, `uKmCard`)는 원래 `ek>sk` 조건이 있어서 정상이었다.
  화면마다 값이 달랐던 이유가 이거였다.

기존에 잘못 저장된 데이터를 고치는 마이그레이션은 넣지 않았다. 저장값 자체는 멀쩡하고
(시작 km은 진짜 시작 km이다) 읽을 때 계산만 틀렸던 거라, 종료 km을 나중에 채워 넣으면
그날 거리가 알아서 맞는다.

---

## 2026-08-01 — 업데이트 알림 팝업, 설정탭 내역

이 앱을 형님과 같이 쓰는데, 기능을 추가하거나 버그를 고쳐도 상대방은 알 방법이 없었다.
말로 전하지 않으면 바뀐 줄도 몰랐다.

- **앱을 열 때 새 버전이면 팝업**으로 뭐가 바뀌었는지 알려준다. **최신 항목 하나만** 띄운다
  (놓친 건 설정탭에서 본다).
- **설정탭 '업데이트 내역'** 에 전체가 쌓인다. 현재 버전도 같이 보여준다.

`CHANGELOG` 배열 하나만 관리하고 `APP_VERSION = CHANGELOG[0].v` 로 끌어온다.
버전을 따로 올리는 걸 잊는 실수를 없애려고 이렇게 묶었다. 문구는 **운전기사가 읽을 말**로
쓴다 — `blob`, `preventDefault` 같은 말은 안 쓴다.

팝업을 띄우는 규칙:

- `cfg.seenVersion` 이 `APP_VERSION` 과 다르면 띄운다
- **갓 설치한 사람에게는 안 띄운다** (기록이 하나도 없으면 신규로 본다). 처음 쓰는 사람에게
  "뭐가 바뀌었어요"는 의미가 없다
- **닫아야만 `seenVersion` 이 저장된다.** 실수로 앱이 꺼져도 다음에 다시 뜬다

같이 고친 것: **배포해도 앱을 두 번 여닫아야 새 버전이 뜨던 문제.**
서비스워커가 cache-first 라, 앱을 열면 캐시의 옛 파일을 먼저 보여주고 새 파일은 뒤에서
받아둔다. 그래서 팝업도 한 박자 늦게 떴을 것이다. `index.html` 의 등록 코드에 업데이트
감지를 붙여서, 새 버전을 다 받으면 하단 탭바 위에 **'새 버전이 준비됐어요 · 새로고침'**
띠를 띄운다. **자동 새로고침은 하지 않는다** — 기록을 쓰던 중이면 입력이 날아간다.
`sw.js` 는 이미 `skipWaiting()` + `clients.claim()` 이라 새로고침만 하면 새 파일이 뜬다.

모달 컴포넌트가 없어서 새로 만들었다. `showToast()` 는 1.4초 뒤 자동으로 사라져서
읽을 시간이 필요한 변경 내역에는 못 쓴다.

배포할 때 올릴 곳이 세 군데(`CHANGELOG`·`sw.js` 캐시·`index.html` `?v=`)라
**`CLAUDE.md` 에 체크리스트를 박아뒀다.** 하나라도 빠지면 조용히 안 바뀐다.

검증은 Playwright(375×667)로 22항목. 옛 버전 사용자·신규 설치·기존 사용자 각각의 분기,
확인/배경 탭으로 닫기, 카드 안 탭은 안 닫히는 것, 닫은 뒤 새로고침해도 다시 안 뜨는 것,
문구에 `<img onerror=...>` 를 넣어 이스케이프되는 것, 알림 띠가 하단 탭바를 안 가리는 것까지
확인했다. 기존 회귀 10항목도 다시 돌렸다.
**서비스워커 실제 업데이트 흐름은 로컬에서 재현이 까다로워 실기기 확인이 필요하다.**

---

## 2026-08-01 — 달력에서 옆으로 쓸면 앱이 튕기던 버그

아이폰에서 달력을 왼쪽으로 쓸면 앱이 사라지고 사파리 오류 화면이 떴다.

```
Safari가 해당 페이지를 열 수 없습니다.
오류: '작업을 완료할 수 없습니다. (WebKitBlobResource 오류 1.)'
```

**같은 날 한 달력 작업과는 무관한, 6월부터 있던 버그다.** `git log -S` 로 확인했다 —
`doBackup()` 은 `4722515`(06-18), 스와이프 핸들러는 최초 `app.js` 부터 그 형태였다.

원인은 `doBackup()` 하나였다. 앱에서 화면을 밖으로 이동시키는 곳이 거기뿐이다.

```js
a.href=url; a.download=filename; a.target='_blank';   // ← 이것
setTimeout(function(){URL.revokeObjectURL(url);},3000);
```

`manifest.json` 이 `display:standalone` 이라, 이 상태에서 `target='_blank'` 로 blob
주소를 열면 iOS가 그 주소를 히스토리에 남긴다. 3초 뒤 `revokeObjectURL()` 이 blob을
없애면 그 항목은 죽은 주소가 된다. 그리고 **standalone PWA에서 가장자리 가로
스와이프는 iOS의 뒤로/앞으로 가기**다. 달력 스와이프 핸들러가 `{passive:true}` 에
`preventDefault()` 도 없어서 iOS 제스처를 전혀 막지 못하니, 달력을 넘기려던 손짓이
그대로 "앞으로 가기"가 되어 죽은 blob 주소로 돌아갔던 것이다.

고친 것:

- **백업을 공유 시트(`navigator.share`)로 바꿨다.** 아이폰에서 공유창이 떠서
  '파일에 저장'·카톡·메일을 고를 수 있고, **화면 이동이 없어 히스토리도 안 남는다.**
  공유 시트를 못 쓰는 브라우저는 `dlBackup()` 으로 떨어진다 — 여기서도
  **`target` 을 주지 않는다.** 이게 이 버그의 전부라 주석으로 크게 남겼다.
- **달력 가로 스와이프에 `touchmove` `{passive:false}` 를 붙여 `preventDefault()`** 한다.
  가로로 12px 넘게 움직여 스와이프로 확정된 뒤에만 막는다. 가장자리 24px를 통째로
  막는 흔한 방법은 일요일·토요일 칸 탭을 죽여서 쓰지 않았다.
  달력 탭은 세로 스크롤이 없어(`.cgrid{overflow:hidden}`) 스크롤을 해치지 않고,
  당겨서 새로고침은 세로라 영향이 없다.

**이건 보조 장치고 진짜 수정은 백업 쪽이다.** iOS는 가장자리 제스처를 이르게
판정해서, 화면 맨 끝에서 시작한 스와이프는 `preventDefault()` 로도 못 막을 수 있다.

검증은 Playwright(375×667 터치)로 했다. 백업 시 팝업·네비게이션 0건, 좌우 스와이프
월 넘김, **일요일·토요일 칸 탭**(이번 변경의 유일한 회귀 위험), 당겨서 새로고침,
`preventDefault` 가 달력 가로에만 10/10 걸리고 세로·다른 탭엔 0건인 것까지 확인했다.
**iOS 실기기는 이 환경에서 재현할 수 없어 최종 확인은 사용자 몫으로 남긴다.**

이미 히스토리에 박힌 죽은 항목은 배포해도 남는다. 앱을 완전히 종료했다 다시 열면
웹뷰 히스토리가 지워지면서 사라진다.

작업 중 딴 데로 샜던 것: 헤드리스 Chromium이 한글 파일명 다운로드를 `download` 로
보고하길래 `target='_blank'` 탓인 줄 알았는데, 영문명으로 바꿔 재현해보니 그냥
한글 파일명 문제였다. 이 버그와 무관하고 아이폰 공유 시트에는 이름이 제대로
들어가므로 파일명은 그대로 뒀다.

---

## 2026-08-01 — 달력에 공휴일·음력·절기, 음력 기념일 등록

달력을 볼 때마다 공휴일과 음력을 확인하러 다른 앱을 열어야 했다. 처음 요청은
"네이버 캘린더를 연동해달라"였는데, **네이버는 외부 앱이 캘린더나 공휴일을 읽어갈
공개 API를 제공하지 않는다.** 대안으로 공공데이터포털 특일정보 API와 구글 공휴일
캘린더도 봤지만 둘 다 API 키가 필요하고, GitHub Pages에서 CORS에 막힐 가능성이 크고,
무엇보다 **오프라인에서 안 된다.** 이 앱은 `fetch` 한 줄 없이 localStorage로만
돌아가는 완전 오프라인 PWA라 구조가 맞지 않는다.

그래서 **2024~2035년 12년치 데이터를 앱에 내장**하는 쪽으로 갔다. 네트워크를 전혀
쓰지 않으니 지하 현장에서도 뜨고, API가 죽거나 키가 만료될 걱정이 없다. 대신
2036년부터는 데이터를 새로 넣어줘야 한다 (범위 밖은 조용히 표시만 생략한다).

`js/kcal.js` 를 새로 만들었다. `app.js` 가 이미 1000줄이라 순수 데이터·계산만 분리했다.

- **음력** — 연도별로 `{설날 양력, 윤달 번호, 월별 일수 비트열}` 만 저장하고 계산으로
  풀었다. 12년치가 1KB도 안 된다.
- **공휴일** — 대체공휴일 규칙을 코드로 구현할까 하다가 **표로 박았다.** 규칙만으로는
  2025-01-27 임시공휴일이나 선거일을 만들 수 없고, 규칙 구현 버그가 곧 틀린 빨간날이
  되기 때문이다. 2026년 제헌절 재지정도 반영돼 있다.
- **절기·복날** — 24절기는 태양 황경 15° 간격으로 계산했고, 삼복은 하지·입추 기준
  庚일로 뽑았다.

데이터가 이 작업의 전부라, **독립된 두 소스(KASI 기반 korean_lunar_calendar, holidays
패키지)로 4370일 전부를 대조**했고 불일치 0건이었다. 논쟁이 있는 2033년 윤11월도
양쪽이 같았다. `kcSelfTest()` 를 파일에 남겨뒀다 — 월일수 합·양음력 왕복·명절 36건을
검사한다. 데이터를 손대면 콘솔에서 돌려볼 것.

화면 쪽:

- 날짜 숫자 옆에 작은 음력, 공휴일은 숫자가 빨갛고 아래에 이름이 붙는다.
- **음력은 초하루(음 1일)·보름(음 15일)에만 찍는다.** 처음엔 모든 날에 넣었는데 칸이
  빽빽해져서 줄였다. 달이 바뀌는 날과 한가운데만 남기면 양력 한 달에 보통 두 번
  (음력 달이 29일이면 세 번) 나온다. 나머지 날의 음력은 날짜를 눌러 상세 헤더에서 본다.
- **칸 높이가 고정이라 넘치는 내용은 그냥 잘린다.** 기록 배지가 이미 두 줄 이상인 날은
  이름 줄을 생략하고 날짜 색으로만 알린다. 이름은 날짜를 눌러 상세 헤더에서 본다.
  같이 `.dn` 을 26→22px 로 줄여서, 배지가 꽉 찬 날은 오히려 전보다 덜 잘리게 됐다.
- 설정에 **음력 기념일(생일·기일)** 등록을 넣었다. 음력으로 넣어두면 해마다 바뀌는
  양력 날짜에 자동으로 뜬다. 양력 등록도 된다.
- **내가 등록한 기념일을 공휴일보다 앞에 둔다.** 공휴일은 날짜가 빨간 것만으로도
  알 수 있지만 내 기일은 여기 말고 알 길이 없다. 상세 헤더에는 둘 다 보여준다.
- 절기·기념일은 칸이 좁아 **기본 꺼둠**. 설정에서 켤 수 있다.
- 하드코딩돼 있던 주말 색(`#e05555` / `#5588dd`)을 `--th-holiday` / `--th-sat` 변수로
  뺐다. 공휴일도 같은 변수를 쓴다.

새 파일이 생겼으니 `sw.js` 캐시를 v9→v10 으로 올리고 `FILES` 에 `kcal.js` 를 넣었다.
안 올리면 기존 설치 기기가 새 파일을 404로 받는다.

작업 중 `rCal()` 의 `m` 이 0-based인데 `kcLunarStr()` 에 그대로 넘겨서 음력이 한 달씩
밀려 있었다. 스크린샷을 찍어 보고 잡았다. 그 뒤로 **144개 달 전부를 렌더해 칸에 그려진
음력과 공휴일 클래스를 대조**하는 검사를 돌렸다.

---

## 2026-07-27 — 텍스트 크기 1.25배 확대

이 앱을 같이 쓰는 형님이 글자가 작아 보기 힘들다고 해서 키웠다. 운전 중이거나
현장에서 잠깐 확인하는 상황이라 글자 크기가 곧 사용성이다.

두 가지 안을 만들어 비교한 뒤 **B안(비율 유지 1.25배)** 으로 정했다.
A안은 작은 글자를 20px까지 끌어올려 평평하게 맞추는 쪽이었는데, 큰 숫자와의
강약이 사라져서 채택하지 않았다.

- 헤더 20→25px, 부제목 12→15px, 섹션 제목 14→18px
- 통계 카드 라벨 9→12px, 숫자 20→25px, 단위 11→14px
- 표 항목명 11→14px, 값 13→16px, 강조 값 15→19px
- 입력 필드 14→17px — 덤으로 iOS에서 입력칸 누를 때 화면이 확대되던 것도 사라졌다
  (16px 미만이면 iOS가 자동 확대한다)
- 하단 네비 10→13px, 아이콘 21→25px, 늘어난 높이만큼 본문 하단 여백 보정
- 달력 하단 요약바 라벨 10→13px, 숫자 16→20px

**달력 날짜 그리드는 그대로 뒀다.** 한 칸에 날짜·바리수 배지·주유/OT/톨비/폐수 배지가
같이 들어가서, 글자를 키우면 배지가 밀려난다.

글자를 키우니 "🔴 잔량 182.2L — 도급비 차감" 같은 긴 값이 두 줄로 넘어갔다.
값마다 크기를 손으로 정하면 금액 자릿수가 바뀔 때 또 깨지므로, 렌더링할 때마다
넘치는 값만 한 줄에 들어갈 때까지 0.5px씩 줄이는 `fit1()`을 넣었다. 원래 크기는
`data-fit-base`에 보관해서 값이 짧아지면 다시 커진다. 연말결산 월별 상세표는
6열이라 줄여도 한계가 있어 가로 스크롤로 바꿨다.

작업 중 월/연 수령액 인라인 스타일에 `px`가 두 번 붙어(`font-size:20pxpx`) 크기 지정이
통째로 무시되던 것을 발견해 함께 고쳤다.

## 2026-07-22 — 디자인 개편, 현금거래 기능

일반 현장 잔량 판매를 기록하는 현금거래 항목을 추가했다. 현장 위치·담당자·금액·이동거리를
남기고, 이동거리는 전체 누적 km에 반영된다.

## 2026-07-06 — 시작 km 자동 입력 개선, 오티 수당 상세, 자동 배포

오티 수당을 합계만 보여주던 것을 날짜별 상세 내역까지 펼치도록 바꿨다.
GitHub Actions로 `main`에 푸시하면 Pages에 자동 배포되도록 워크플로를 붙였다.

## 2026-07-01 ~ 07-02 — 월별 단가, 전날 종료 km 자동 입력

바리당 단가가 월마다 달라져서 `monthlyUnitPrices`로 월별 단가를 따로 저장하게 했다.
기본 단가는 신규 월의 시작값으로만 쓴다.

시작 km를 매번 손으로 넣는 게 번거로워 전날 종료 km를 자동으로 채우도록 했다.
폐수 단가 설정과 통계 반영, 일별 톨비 내역 렌더링도 이때 들어갔다.
달력이 튕기던(bounce) 문제와 pull-to-refresh 버그도 함께 잡았다.

## 2026-06-27 ~ 06-30 — 모바일 조작감, 기록 탭 제거

**기록 탭을 없앴다.** 달력에서 날짜를 눌러 바로 입력하는 흐름이면 충분해서,
탭 하나를 통째로 덜어냈다.

확대 관련해서 두 번 방향을 바꿨다. 처음엔 pinch zoom을 허용했다가(6/27),
운전 중에 의도치 않게 확대되는 일이 많아 두 손가락·더블 탭 확대를 다시 막았다(6/30).
지금 `index.html`의 `maximum-scale=1.0, user-scalable=no`가 그 결과다.

pull-to-refresh를 넣고, 연말결산의 월 평균 계산 오류를 고쳤다.
이 시점에 `DESIGN.md`를 처음 만들어 테마 토큰과 배지 색 규칙을 문서로 뽑았다.

## 2026-06-18 — 테마 두 가지로 단순화

테마가 여러 개(dark/red/green/orange/purple/kakao/linear)였는데 실제로는 거의
쓰지 않아서 **SpaceX(기본)과 화이트 두 가지로 줄였다.**

한 번에 가지는 못했다. 단순화했다가(`57aec01`) 되돌리고(`8224f77`) 다시 적용했다(`4722515`).
줄이고 나니 화이트 테마에서 글자가 잘 안 보이는 문제가 드러나서 `--on-primary-mute`를
추가하고, 설정 섹션 제목 색을 더 진하게 잡았다. 어두운 테마 기준으로 짜둔 색을
밝은 배경에 그대로 쓰면 안 된다는 걸 여기서 알았다.

iOS 백업 버그도 이때 고쳤다.

## 2026-06-08 ~ 06-10 — SpaceX 테마, PWA, 그리고 사진 기능 철회

SpaceX에서 따온 다크 테마로 전면 개편했다. 순수 검정 대신 표면 층이 구분되는
다크 그레이 스케일을 쓴다. 서비스워커를 붙여 오프라인에서도 열리게 했고,
정비 이동거리 기능을 추가했다.

**사진 첨부 기능은 넣었다가 뺐다.** 현장 사진을 남기면 좋겠다 싶어 자동 갤러리 저장과
압축까지 붙였는데(6/9), 저장 오류가 나서 자동 갤러리 저장을 먼저 걷어내고,
결국 **localStorage 용량 초과** 때문에 기능 전체를 제거했다(6/10). 이미지를 base64로
넣는 순간 5MB 한도가 금방 찬다. 지금도 데이터는 전부 localStorage에 있으므로
같은 이유로 이미지 계열 기능은 다시 넣지 않는다.

## 2026-06-03 ~ 06-04 — 파일 분리

한 파일에 다 들어 있던 `레미콘운행일지.html`을 `index.html` + `js/app.js` + `css/style.css`로
쪼갰다. 이때부터 지금의 구조가 됐다.

## 2026-05-21 ~ 05-31 — 시작

`레미콘운행일지.html` 파일 하나로 시작했다. GitHub 웹에서 직접 파일을 올리고 고치는
방식이었다("Add files via upload", "Update 레미콘운행일지.html"). 빌드도 프레임워크도 없이
바닐라 JS로 짠 건 이때 정한 방향이고, 지금까지 그대로 간다.

---

## 이 문서 갱신 규칙

기능을 추가하거나 동작을 바꾸는 작업을 하면 이 문서 맨 위(`한눈에 보기` 표 아래)에
날짜 항목을 새로 붙인다. 되돌린 시도도 지우지 말고 남긴다.

오타 수정이나 값 하나 바꾸는 정도는 적지 않는다. 나중에 "왜 이렇게 돼 있지"를
물을 만한 변경만 남긴다.
